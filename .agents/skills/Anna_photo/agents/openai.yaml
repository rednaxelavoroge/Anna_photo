interface:
  display_name: "Anna Photo"
  short_description: "Repo-specific patterns and workflows for Anna_photo"
  default_prompt: "Use the Anna_photo repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true